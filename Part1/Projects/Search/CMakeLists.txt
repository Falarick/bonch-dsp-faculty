CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(Convolution C)

SET(EXEC_TARGET search)

SET(CMAKE_C_STANDARD 11)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR}/include)

# SET(HEADERS ...)

SET(SOURCES 
    ./source/main.c)

ADD_EXECUTABLE(${EXEC_TARGET} ${SOURCES})